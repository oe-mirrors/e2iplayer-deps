# Store text as LF in the repo. Git converts for the working tree per
# platform on checkout; every committed blob is LF.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.so  binary
*.a   binary
*.o   binary
*.bin binary
